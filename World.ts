import {ActorClassRoot as ACR} from "./ActorClassRoot"

class World
{
    Player: ACR;
    Alicia: ACR;
    Ragan: ACR;
    Sarai: ACR;
    PlayerParty: ACR[];
    constructor(firstWname = "", mapseto: any[][][]) 
    {
        enum rolex 
        {
            Admin = 0, Player = 1, HAI = 2, EAI = 3, YaMum = 4
            //EAI: is enemy
            //HAI: is party AI member
            //Admin: is a you don't fck with person
            //YaMum: same as admin above
        }
        
    };
}

function TextReader(fileInx: File)
{
    fileFR: FileReader = FileReader;
    fileInx.stream;
}

function adLib(lorem: string, ipsum: string, marko: string)
{
    // lorem is for words to make sentences
    // ipsum is for anything relatively arbritrary for the design
    // of the dialoge in order to work.
    // marko is for context. A pre-filled in the blank if u will.

    myWorldWords: String[300]; // lorem
    myWorldDialoge: String[100]; // ipsum
    myWorldContext: String[40]; // marko 

    console.log(lorem + ipsum + marko + ipsum + lorem);
}

function EventMachine(Chap: number, opDialoge: string) 
{
    return Chap + opDialoge;
}

function dialougeW1(anyd:any, starterLines = "") 
{
    
}

function randomDC(lorem:string[], ipsum:string[], marko:string[], randomocity:number, randomocity2:number, randomocity3:number) 
{
    randomocity = 0, 300;
    randomocity2 = 0, 100;
    randomocity3 = 0, 40;
    console.log(marko[randomocity3] + ipsum[randomocity2] + ipsum[randomocity2] + lorem[randomocity] + lorem[randomocity] + lorem[randomocity] + marko[randomocity3]);
}

function World1(firstWname = "", mapseto: any[][][]): void 
{
    
}

World1("Pallidium Outskirts", new[1][0][0]);